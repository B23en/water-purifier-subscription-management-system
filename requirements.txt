# Python 3.11 기준 / 현재 검증된 버전으로 고정
streamlit==1.34.0
openai==1.28.1
# openai 1.28.x 는 httpx>=0.28 에서 클라이언트 생성 시 'proxies' 인자 충돌로 깨짐 → 고정 필수
httpx==0.27.0
python-dotenv==1.0.1
pandas==2.2.1
pyarrow==16.0.0
matplotlib==3.8.3
openpyxl==3.1.5
